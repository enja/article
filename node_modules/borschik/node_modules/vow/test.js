var vow = require('./lib/vow');

var thenable = Object.create(null, {
    then: {
        get: function () {
            return function thenMethodForX(onFulfilled) {
                console.log('!');
                onFulfilled();
            };
        }
    }
});

thenable.resolve('!')